<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissões -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppLogicando"
        tools:targetApi="31">

        <!-- Tela de cadastro -->
        <activity android:name=".CadastroActivity" />

        <!-- Tela principal do aluno -->
        <activity android:name=".AlunoMainActivity" />

        <!-- Tela principal do professor -->
        <activity android:name=".ProfessorMainActivity" />

        <!-- Tela Criar Formulario-->
        <activity android:name=".CriarFormularioActivity" />

        <!-- Tela Criar Perguntas-->
        <activity android:name=".CriarPerguntaActivity" />

        <!-- tela ver formularios-->
        <activity android:name=".ListarFormulariosActivity" />

        <!-- Tela para visualizar respostas -->
        <activity android:name=".VisualizarRespostasActivity" />

        <!-- Lista de formulários para alunos -->
        <activity android:name=".ListaFormulariosAlunoActivity" />

        <!-- Tela de login (inicial do app) -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
